# Configurations

Store reusable benchmark case definitions and shared settings here. A case
should make its operator, shapes, dtypes, layouts, tolerances, warmup count,
measurement count, and any backend-specific constraints clear.

Backend-only configuration may live below the corresponding backend directory.

