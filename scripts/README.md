# Scripts

Put repository-level automation here, such as running a selected set of
backends, validating result files, or aggregating reports. Scripts should
accept explicit paths and backend names rather than relying on the current
working directory.

