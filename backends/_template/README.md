# Backend template

Copy this directory to `backends/<backend_name>` when adding a backend. Keep
backend-specific operator code in `ops/` and benchmark entry points in
`benchmarks/`.

Add a backend README after the first implementation lands, including supported
devices, dependencies, setup steps, and example commands.

